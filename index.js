const endpoints             = require('./libs/api-endpoints');
const urls                  = require('./libs/urls');
const requestPrototypes     = require('./libs/request-prototypes');
const parsex                = require('./libs/parsex');
const defaultLocalConfig    = require('./libs/default-local-config');
const stringify             = require('./libs/stringify');

module.exports = {
    endpoints,
    urls,
    requestPrototypes,
    parsex,
    defaultLocalConfig,
    stringify
}
